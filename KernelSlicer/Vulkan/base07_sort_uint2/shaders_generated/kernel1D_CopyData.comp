#version 460
#extension GL_GOOGLE_include_directive : require

#include "common_generated.h"
layout(binding = 0, set = 0) buffer data0 { uvec2 a_in[]; }; // 
layout(binding = 1, set = 0) buffer data1 { uvec2 a_out[]; }; // 
layout(binding = 2, set = 0) buffer dataUBO { Sorter_Generated_UBO_Data ubo; };



layout(local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

layout( push_constant ) uniform kernelArgs
{
  int a_size; 
  uint iNumElementsY; 
  uint iNumElementsZ; 
  uint tFlagsMask;    
} kgenArgs;



void main()
{
  bool runThisThread = true;
  const int i = int(gl_GlobalInvocationID[0]); 
  if(i >= kgenArgs.a_size + 0)
    runThisThread = false;
    if(runThisThread)
  {
  
    a_out[i] = a_in[i];
  
  }
}

